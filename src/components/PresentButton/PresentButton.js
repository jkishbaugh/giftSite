import React from 'react';
import styles from './PresentButton.module.css'

const PresentButton = () => {
    return (
        <div className={styles.PresentButton}>
            Button
        </div>
    )
}

export default PresentButton;